{
  "name": "device-fov-identificator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host 0.0.0.0 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:watch": "vitest",
    "setup:https": "bash scripts/setup-https.sh"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vitejs/plugin-basic-ssl": "^2.1.4",
    "jsdom": "^28.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  }
}
